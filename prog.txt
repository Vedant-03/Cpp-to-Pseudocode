void level_order_traversal(Node root, int n, string s){
if(root==NULL){
return;
}
queue<Node> nodeQueue;//queue of nodes
enqueue(nodeQueue, root);
while(!nodeQueue.empty()){
node = nodeQueue.front();
cout<<d[node]; //data = d. parent = NULLL
dequeue(nodeQueue);
if(l[node] != NULL){
enqueue(nodeQueue, l[node]);
}
if(r[node] != NULL){
enqueue(nodeQueue, r[node]);
}
cout<<a;
}
}